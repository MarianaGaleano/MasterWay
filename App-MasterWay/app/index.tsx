import Login from "@/components/Login";
import { Text, View } from "react-native";

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <Login />
    </View>
  );
}
